# Formspree id, GA4 id, and Meta Pixel id now live in src/config/site.ts —
# they're public by design (they compile into static HTML), so there's no
# env var for them anymore.

# Set once the LiteLLM gateway is live to switch the playground from scripted demo to real inference
PUBLIC_GATEWAY_URL=
