---
import { getCollection } from 'astro:content';
import Site from '../../layouts/Site.astro';
import ArticleGrid from '@justinguese/astro-kit/ArticleGrid.astro';
import SectionHeader from '@justinguese/astro-kit/SectionHeader.astro';

const dokumente = (await getCollection('unterlagen')).sort((a, b) => a.data.reihenfolge - b.data.reihenfolge);

const title = 'Unterlagen | Nachweis';
const description = 'AVV, TOM, Subprozessorenliste, §203-Zusatzvereinbarung und VVT-Muster — ungefiltert, ohne Formular.';
---

<Site title={title} description={description}>
  <section class="py-14 sm:py-20 border-b border-line">
    <div class="max-w-3xl mx-auto px-4 sm:px-6 lg:px-8">
      <SectionHeader
        as="h1"
        eyebrow="Unterlagen"
        title="Für Ihren Datenschutzbeauftragten — ohne Anmeldung."
        lead="Diese fünf Dokumente reichen aus, um eine erste Einschätzung zu treffen, ob Nachweis für Ihre Kanzlei oder Praxis infrage kommt. Kein Formular, keine E-Mail-Pflicht — genau deshalb, weil das der Punkt ist: Ihr DSB soll das prüfen können, bevor irgendjemand mit Ihnen spricht."
        size="lg"
      />
    </div>
  </section>

  <section class="py-12 sm:py-16">
    <div class="max-w-6xl mx-auto px-4 sm:px-6 lg:px-8">
      <ArticleGrid
        columns={3}
        items={dokumente.map((doc) => ({
          href: `/unterlagen/${doc.id}`,
          title: doc.data.title,
          description: doc.data.kurz,
          meta: `${doc.data.version} · Stand: ${doc.data.stand}`,
          cta: 'Ansehen',
        }))}
      />
    </div>
  </section>
</Site>
