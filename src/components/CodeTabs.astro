---
interface Tab {
  label: string;
  code: string;
}

interface Props {
  tabs: Tab[];
}

const { tabs } = Astro.props;
const groupId = `code-${Math.random().toString(36).slice(2, 8)}`;
---

<div class="border border-line-deep bg-ink-deep" data-code-tabs={groupId}>
  <div class="flex border-b border-line-deep">
    {tabs.map((tab, i) => (
      <button
        type="button"
        data-code-tab={i}
        class:list={[
          'px-4 py-2.5 text-xs font-mono uppercase tracking-wider transition-colors border-r border-line-deep last:border-r-0',
          i === 0 ? 'text-accent-bright bg-ink' : 'text-ink-faint hover:text-canvas',
        ]}
      >
        {tab.label}
      </button>
    ))}
  </div>
  {tabs.map((tab, i) => (
    <pre
      data-code-panel={i}
      class:list={['px-5 py-4 text-sm font-mono text-[#dbe2ef] overflow-x-auto leading-relaxed', i !== 0 && 'hidden']}
    ><code>{tab.code}</code></pre>
  ))}
</div>

<script>
  function initCodeTabs() {
    document.querySelectorAll<HTMLElement>('[data-code-tabs]').forEach((group) => {
      if (group.dataset.init) return;
      group.dataset.init = 'true';
      const buttons = group.querySelectorAll<HTMLButtonElement>('[data-code-tab]');
      const panels = group.querySelectorAll<HTMLElement>('[data-code-panel]');
      buttons.forEach((btn) => {
        btn.addEventListener('click', () => {
          const idx = btn.dataset.codeTab;
          buttons.forEach((b) => b.classList.remove('text-accent-bright', 'bg-ink'));
          buttons.forEach((b) => b.classList.add('text-ink-faint'));
          btn.classList.add('text-accent-bright', 'bg-ink');
          btn.classList.remove('text-ink-faint');
          panels.forEach((p) => p.classList.toggle('hidden', p.dataset.codePanel !== idx));
        });
      });
    });
  }
  initCodeTabs();
  document.addEventListener('astro:after-swap', initCodeTabs);
</script>
